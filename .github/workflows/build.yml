name: Build

on:  
  push:
  workflow_dispatch:
     inputs:
      jobId:
        description: 'Job ID'
        required: true
        default: 'dryRun'

jobs:
  windowsBuildForAllSupportedPlatforms:
    name: Test Run
    runs-on: windows-2019
      
    steps:
      - run: |
          if ("")
          {
            echo hello
          } else
          {
            echo goodbye
          }
