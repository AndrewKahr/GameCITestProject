name: Build

on:  
  push:
  workflow_dispatch:

jobs:
  run:
    name: Run
    runs-on: ubuntu-latest  
    steps:
      - run: |
          echo ${{ github.sha }}
          echo git rev-parse HEAD
          echo git rev-list --count ${{ github.sha }}
          echo git rev-list --count HEAD
